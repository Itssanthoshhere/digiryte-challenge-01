{
  "name": "digiryte-challenge-01-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Digiryte Challenge 01 - Secure REST API (Node.js/Express) + React Frontend",
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm install --include=dev --prefix client && npm --prefix client run build && npm install --include=dev --prefix backend && npm --prefix backend run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "workspaces": [
    "backend",
    "client"
  ]
}
