{
  "name": "digiryte-challenge-01-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Digiryte Challenge 01 - Secure REST API (Node.js/Express) + React Frontend",
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix backend run build && npm --prefix client run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "workspaces": [
    "backend",
    "client"
  ]
}
